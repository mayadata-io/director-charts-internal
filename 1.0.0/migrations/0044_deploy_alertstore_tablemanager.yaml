apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: alertstore-tablemanager
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: alertstore-tablemanager
    spec:
      containers:
      - name: alertstore-tablemanager
        image: moteesh/alertstore-tablemanager:staging
        imagePullPolicy: Always
        args:
        - -server.http-listen-port=80
        - -storage.storage-client=cassandra
#        - -cassandra.addresses=172.17.0.4
        - -cassandra.addresses=cassandra.default.svc.cluster.local
        - -cassandra.keyspace=mayaalerts
#        - -cassandra.table-duration=5m
#        - -tablemanager.table-duration=5m
        - -log.level=debug
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: alertstore-tablemanager
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    name: alertstore-tablemanager

